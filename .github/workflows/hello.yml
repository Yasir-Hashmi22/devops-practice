name : my first CI Pipeline

on : push

jobs:
  hello-job:
    runs-on : ubuntu-latest

    steps:
    -name: checkout code
     uses: actions/checkout@v4

    -name: Say Hello
      run: echo "Hello Atique! , Your pipeline is Working "

    -name: show date and Time
      run: date
    -name: list files in repo
      run: ls -la
